#include "main.h"
#include <stdio.h>
/**
  * main - Entry point
  * @argc: number of args
  * @argv: array of args
  * Return: Always 0 on (Success)
  */
int main(int argc, char *argv[] __attribute__ ((unused)))
{
	printf("%d\n", argc - 1);
	return (0);
}
